/*
 * Some Example program
 */
int main(int argc, char **argv){
	char *filename;
	filename = "input.txt";
	for (int i = 0; i < argc; i++){
		if (strcmp(argv[i],"-i"){
			filename = argv[i + 1];
			i++;
		} else { 
		// do nothing
		}
	}
	float a = (6 % 11 + 3.0 - .12 * .42e4 / 2) < 1.0e-5;
	char c = 'q';
	void *ptr;
	while ( a == 4 || 0 <= a && 1 >= a)
	return 0;
}